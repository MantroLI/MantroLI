;(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined'
    ? (module.exports = factory())
    : typeof define === 'function' && global.define.amd
    ? define(factory) // eslint-disable-line
      : (global.__ce = factory())
})(global, function () {
  return {
    _: {},
    debug: true,
    baseURL: 'http://1711010009.dev-pool1-ceshop.yun300.cn/',
    invoiceBaseURL: 'http://1711010009.dev-pool1-ceshop.yun300.cn'
  }
})
